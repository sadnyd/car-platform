server:
  port: 8082

spring:
  application:
    name: inventory-service

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
